import 'package:flutter/material.dart';

class FinishQuote extends StatelessWidget {
  const FinishQuote({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.purple,
    );
  }
}
